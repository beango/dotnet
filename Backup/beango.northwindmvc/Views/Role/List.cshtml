@model IEnumerable<beango.model.Role>

@{
    ViewBag.Title = "角色列表";
}

<h2 style="float: left;">角色列表</h2>
<p style="position: relative; height: 36px;">
    @Html.ActionLink("Create New", "Create", null, new { style = "position:absolute; margin-left:10px; bottom:6px;" })
</p>
<table class="table table-striped">
    <tr>
        <th>
            RoleName
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RoleName)
        </td>
        <td>
            @Html.ActionLink("权限", "RoleAuth", new { id=item.RoleID }) |
            @Html.ActionLink("Edit", "Edit", new { id = item.RoleID }) |
            @Html.ActionLink("Details", "Details", new { id = item.RoleID }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.RoleID })
        </td>
    </tr>
}

</table>
